% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_data.R
\name{plot_species_on_map}
\alias{plot_species_on_map}
\title{Plot Latitude and Longitude of Species on a Map of India}
\usage{
plot_species_on_map(
  species_names,
  custom_colors = NULL,
  palette = "viridis",
  opacity = 0.7,
  highlight = FALSE
)
}
\arguments{
\item{species_names}{Character vector. Names of species to plot. Must match
\code{acc_species_name} in the database.}

\item{custom_colors}{Optional. Named vector of colors for each species. If
provided, length must match \code{species_names}.}

\item{palette}{Optional. Character string specifying a palette for colors.
Options include \code{"rainbow"}, \code{"heat"}, \code{"terrain"}, \code{"topo"}, \code{"cm"},
\code{"viridis"}, \code{"plasma"}, \code{"inferno"}, \code{"magma"}, \code{"cividis"}, \code{"green"},
\code{"red"}, \code{"purple"}, \code{"orange"}, \code{"yellow"}, \code{"brown"}, \code{"grey"},
\code{"diverging1"}, \code{"diverging2"}, \code{"diverging3"}, \code{"diverging4"}.}

\item{opacity}{Numeric. Opacity of the plotted points (0 to 1). Default 0.7.}

\item{highlight}{Logical. If TRUE, highlights regions containing species occurrences. Default FALSE.}
}
\value{
A \code{ggplot} object displaying the species occurrences on a map of India.
}
\description{
Visualizes occurrences of one or more species on a map of India using their
latitude and longitude data from the ILORA database.
}
\details{
Plots multiple species occurrences with either a specified palette or
custom colors. Ensures spatial coordinates align with India's shapefile CRS.
}
\examples{
\donttest{
# Plot multiple species with custom colors
plot_species_on_map(
  c("Cyanus segetum Hill", "Cuphea llavea Lex."),
  custom_colors = c("orange", "blue")
)

# Plot a single species using the heat palette and reduced opacity
plot_species_on_map("Quercus robur L.", palette = "heat", opacity = 0.6)

# Plot a species using custom colors and viridis palette
plot_species_on_map("Avena sativa L.", custom_colors = c("orange"), palette = "viridis")
}

}
