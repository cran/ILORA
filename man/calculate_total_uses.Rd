% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_data.R
\name{calculate_total_uses}
\alias{calculate_total_uses}
\title{Calculate Total Number of Uses for Each Species}
\usage{
calculate_total_uses(species = NULL)
}
\arguments{
\item{species}{Optional character vector of species names to filter results.
If \code{NULL} (default), results for all species are returned.}
}
\value{
A data frame with columns:
\itemize{
\item \code{acc_species_name}: The accepted species name.
\item \code{acc_species_id}: Internal species ID.
\item \code{total_uses}: Sum of all recorded uses across relevant economic use columns.
}

If no matching records are found, a message is printed and \code{NULL} is returned.
}
\description{
Computes the total number of uses for species based on the \code{economic_uses} table
in the ILORA database. It sums across all use-related columns for each species.
}
\examples{
\donttest{
# Calculate total uses for all species
all_species_uses <- calculate_total_uses()

# Calculate total uses for specific species
species_list <- c("Quercus robur L.", "Azadirachta indica")
specific_species_uses <- calculate_total_uses(species_list)
}

}
